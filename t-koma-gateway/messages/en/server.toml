[health-status]
body = "OK"

[health-koma]
body = "RUNNING"

[failed-load-interface]
body = "`INTERFACE` load failed. インターフェース読込エラー。"

[interface-invalid-operator]
body = "`INTERFACE` points to invalid `OPERATOR` record."

[failed-load-operator]
body = "`OPERATOR` load failed. オペレータ読込エラー。"

[interface-required]
body = "T-KOMA // This `INTERFACE` requires `OPERATOR BINDING`. Reply `NEW` or `EXISTING`."

[access-pending]
body = "`ACCESS REQUEST` pending. 承認待機中。"

[access-denied]
body = "`ACCESS REQUEST` denied. アクセス拒否。"

[unknown-operator-status]
body = "Unknown `OPERATOR STATUS`. オペレータ状態不明。"

[existing-operator-todo]
body = "`EXISTING OPERATOR` bind path is TODO."

[reply-with-new-or-existing]
body = "Reply with `NEW` or `EXISTING`."

[failed-create-operator]
body = "`OPERATOR` creation failed."

[failed-create-interface]
body = "`INTERFACE` creation failed."

[operator-created-awaiting-approval]
body = "`OPERATOR` shell created. `MANAGEMENT CLI` approval required."

[select-new-or-existing-first]
body = "Select `OPERATOR MODE` first: `NEW` or `EXISTING`."

[failed-list-ghosts]
body = "`GHOST` list query failed."

[no-ghosts-for-operator]
body = "No `GHOST NODE` linked to this `OPERATOR`. Create one from `DISCORD` first."

[unknown-ghost-name-server]
body = "Unknown `GHOST NAME`."

[failed-load-ghost]
body = "`GHOST` record load failed."

[ghost-not-owned]
body = "`GHOST` ownership check failed. このゴーストは別オーナーです。"

[failed-init-ghost-session]
body = "`GHOST SESSION` init failed."

[failed-create-session]
body = "`SESSION` creation failed."

[no-active-ghost]
body = "No `ACTIVE GHOST` selected."

[failed-init-ghost-db]
body = "`GHOST DB` init failed."

[failed-init-session]
body = "`SESSION` init failed."

[invalid-session]
body = "Invalid `SESSION` handle."

[failed-list-sessions]
body = "`SESSION` list query failed."

[failed-switch-session]
body = "`SESSION` switch failed."

[failed-delete-session]
body = "`SESSION` delete failed."

[connected-puppet-master]
body = "`LINK ESTABLISHED`: `T-KOMA CORE` <-> `PUPPET MASTER`"

[connected-logs]
body = "`LOG STREAM` connected to `T-KOMA CORE`"

[error-internal-operator-status]
body = "Internal `STATUS` resolver fault for `OPERATOR`."

[anonymous-operator]
body = "`ANON OPERATOR`"

[rate-limited]
body = "`RATE LIMIT` engaged. Last `MESSAGE` buffered. Send `continue` after {{retry_after}} seconds."
vars = ["retry_after"]
