[discord-interface-prompt]
kind = "choice_prompt"
body = '''
### T-KOMA // インターフェース・バインド
┄┄┄┄┄┄┄┄┄┄┄┄

⚠️ `SECURITY WARNING`
Before linking this `INTERFACE`, you must fully trust the `PUPPET MASTER`
controlling this `T-KOMA` instance.
If you do not fully trust that operator, disconnect now.

This `INTERFACE` must link to exactly one `OPERATOR` (オペレータ).

Select `MODE`:
- **NEW** -> spawn a fresh `OPERATOR PROFILE`
- **EXISTING** -> link an existing `OPERATOR PROFILE`
'''
actions = [
  { id = "new", label = "NEW", intent = "interface.bind.new" },
  { id = "existing", label = "EXISTING", intent = "interface.bind.existing" },
]

[discord-existing-operator-todo]
body = "`EXISTING OPERATOR` linking is not online yet. TODO // トゥードゥー"

[error-failed-create-operator-discord]
body = "`OPERATOR` creation failed. オペレータ生成エラー。"

[error-failed-create-interface-discord]
body = "`INTERFACE` link failed. インターフェース接続エラー。"

[error-failed-load-operator-discord]
body = "`OPERATOR` load failed. オペレータ読込エラー。"

[error-failed-create-session-discord]
body = "`SESSION` bootstrap failed for this `GHOST` (ゴースト)."

[error-init-session-discord]
body = "`SESSION` initialization fault detected. セッション初期化エラー。"
