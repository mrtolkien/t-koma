[model-not-configured]
vars = ["model", "provider"]
body = "`MODEL PROFILE` not configured. `MODEL`='{{model}}' `PROVIDER`='{{provider}}'"

[no-models-configured]
vars = ["provider"]
body = "`MODEL REGISTRY` empty for `PROVIDER` '{{provider}}'."
