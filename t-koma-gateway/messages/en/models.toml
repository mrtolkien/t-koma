[model-not-configured]
vars = ["model", "provider"]
body = "Model '{{model}}' for provider '{{provider}}' is not configured"

[no-models-configured]
vars = ["provider"]
body = "No models configured for provider '{{provider}}'"
